syntax = "proto3";

service Server
{
    rpc getFileCompressed (Chunk) returns (Chunk){}
}

message Chunk
{
    int64          pos             = 1;
    string         path            = 2;
    string         date            = 3;
    int32          buffer          = 4;
    int64          buffer_size     = 5;
}
